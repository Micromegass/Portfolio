node_modules/
dist/
.astro/
.DS_Store
npm-debug.log*

# Local build-time config (endpoints, public keys) — see .env.example
.env
.env.local

# Worker build artefacts
backend/node_modules/
backend/.wrangler/
backend/.dev.vars

# SEO audit: keep the reports (CLAUDE.md links them), not the 3.7MB of
# regenerable screenshots
renovostudio.com-audit/screenshots/
