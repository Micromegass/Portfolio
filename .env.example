# Optional: raises the free quota for the website-check tool.
# Create at https://console.cloud.google.com → enable "PageSpeed Insights API".
# This key is public by design — restrict it by HTTP referrer in the console.
PUBLIC_PSI_KEY=
